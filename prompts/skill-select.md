# Skill Select

Select relevant skills for a task.

1. Read `{vault_path}/skills/_tree.md`.
2. Task: `{task_description}`
3. Return 3-5 most relevant skill file paths, one per line. Return `NONE` if nothing fits.

```
skills/coding/example.md
skills/devops/example.md
```
